@page
@model Unleashed_RP.Pages.Notifications.IndexModel
@{
    ViewData["Title"] = "My Notifications";
}

<h1 class="mb-4">My Notifications</h1>

<div class="card shadow-sm">
    <div class="card-body">
        @if (Model.NotificationUsers == null || !Model.NotificationUsers.Any())
        {
            <div class="alert alert-info text-center" role="alert">
                You have no notifications.
            </div>
        }
        else
        {
            <div class="table-responsive">
                <table class="table table-hover table-striped">
                    <thead class="table-light">
                        <tr>
                            <th>
                                @Html.DisplayNameFor(model => model.NotificationUsers[0].NotificationTitle)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.NotificationUsers[0].NotificationCreatedAt)
                            </th>
                            <th></th>
                    </thead>
                    <tbody>
                        @foreach (var item in Model.NotificationUsers)
                        {
                            <tr class="@(item.IsNotificationViewed.GetValueOrDefault(false) ? "" : "table-primary fw-bold")"
                                onclick="location.href='@Url.Page("./Details", new { id = item.NotificationId })'"
                                style="cursor: pointer;">
                                <td>
                                    @Html.DisplayFor(modelItem => item.NotificationTitle)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.NotificationCreatedAt)
                                </td>                                    
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        }
    </div>
</div>